name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target universal-apple-darwin'
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.22.0

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install dependencies (macOS)
        if: matrix.platform == 'macos-latest'
        run: |
          # Install any system dependencies if needed
          echo "Setting up macOS build environment..."

      - name: Setup Apple code signing (macOS)
        if: matrix.platform == 'macos-latest'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          # Only run if certificate is configured
          if [ -n "$APPLE_CERTIFICATE" ]; then
            echo "🔐 Setting up Apple code signing..."

            # Create certificate files
            echo "$APPLE_CERTIFICATE" | base64 --decode > certificate.p12

            # Create temporary keychain
            security create-keychain -p actions build.keychain
            security default-keychain -s build.keychain
            security unlock-keychain -p actions build.keychain
            security set-keychain-settings -t 3600 -u build.keychain

            # Import certificate to keychain
            security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign -T /usr/bin/productsign

            # Enable codesigning from a non user interactive shell
            security set-key-partition-list -S apple-tool:,apple: -s -k actions build.keychain

            # Verify the identity is available
            security find-identity -v -p codesigning

            echo "✅ Apple code signing setup completed"
          else
            echo "⚠️ No Apple certificate configured - skipping code signing setup"
          fi

      - name: Install dependencies (Windows)
        if: matrix.platform == 'windows-latest'
        run: |
          # Install any system dependencies if needed
          echo "Setting up Windows build environment..."

      - name: Install frontend dependencies
        run: pnpm install

      - name: Download binaries (macOS)
        if: matrix.platform == 'macos-latest'
        run: |
          mkdir -p src-tauri/binaries

          # Download yt-dlp for macOS
          echo "Downloading yt-dlp for macOS..."
          curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_macos -o src-tauri/binaries/yt-dlp-aarch64-apple-darwin
          chmod +x src-tauri/binaries/yt-dlp-aarch64-apple-darwin
          cp src-tauri/binaries/yt-dlp-aarch64-apple-darwin src-tauri/binaries/yt-dlp-x86_64-apple-darwin
          cp src-tauri/binaries/yt-dlp-aarch64-apple-darwin src-tauri/binaries/yt-dlp-universal-apple-darwin

          # Download ffmpeg for macOS
          echo "Downloading ffmpeg for macOS..."
          curl -L https://evermeet.cx/ffmpeg/getrelease/ffmpeg/zip -o src-tauri/binaries/ffmpeg.zip
          cd src-tauri/binaries
          unzip -o ffmpeg.zip
          mv ffmpeg ffmpeg-aarch64-apple-darwin
          chmod +x ffmpeg-aarch64-apple-darwin
          cp ffmpeg-aarch64-apple-darwin ffmpeg-x86_64-apple-darwin
          cp ffmpeg-aarch64-apple-darwin ffmpeg-universal-apple-darwin
          rm ffmpeg.zip

          echo "✅ macOS binaries prepared (aarch64, x86_64, universal)"

      - name: Download binaries (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path src-tauri\binaries

          # Download yt-dlp for Windows
          Write-Host "Downloading yt-dlp for Windows..."
          Invoke-WebRequest -Uri "https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp.exe" -OutFile "src-tauri\binaries\yt-dlp-x86_64-pc-windows-msvc.exe"

          # Download ffmpeg for Windows
          Write-Host "Downloading ffmpeg for Windows..."
          Invoke-WebRequest -Uri "https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip" -OutFile "src-tauri\binaries\ffmpeg.zip"
          Expand-Archive -Path "src-tauri\binaries\ffmpeg.zip" -DestinationPath "src-tauri\binaries\ffmpeg-temp" -Force
          Copy-Item "src-tauri\binaries\ffmpeg-temp\ffmpeg-master-latest-win64-gpl\bin\ffmpeg.exe" "src-tauri\binaries\ffmpeg-x86_64-pc-windows-msvc.exe"
          Remove-Item -Recurse -Force "src-tauri\binaries\ffmpeg-temp"
          Remove-Item "src-tauri\binaries\ffmpeg.zip"

          Write-Host "✅ Windows binaries prepared"

      - name: Build Tauri app (unsigned)
        run: pnpm tauri build ${{ matrix.args }}
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # Don't sign during build - we'll sign manually in the next step
          TAURI_SKIP_CODE_SIGNING: true

      - name: Sign and notarize app (macOS)
        if: matrix.platform == 'macos-latest'
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          echo "🔐 Starting code signing and notarization process..."

          # Debug: List available signing identities
          echo "📋 Available signing identities:"
          security find-identity -v -p codesigning

          # Find the .app bundle
          APP_PATH=$(find src-tauri/target/universal-apple-darwin/release/bundle/macos -name "*.app" -type d | head -n 1)

          if [ -z "$APP_PATH" ]; then
            echo "❌ App bundle not found"
            exit 1
          fi

          echo "Found app at: $APP_PATH"

          # List contents to verify structure
          echo "📁 App bundle contents:"
          ls -laR "$APP_PATH/Contents/Resources/" || true

          # Sign the external binaries first
          RESOURCES_PATH="$APP_PATH/Contents/Resources"

          # Sign all binaries in the binaries directory
          if [ -d "$RESOURCES_PATH/binaries" ]; then
            echo "📝 Signing binaries in $RESOURCES_PATH/binaries/..."
            for binary in "$RESOURCES_PATH/binaries"/*; do
              if [ -f "$binary" ] && [ -x "$binary" ]; then
                echo "  Signing $(basename "$binary")..."
                codesign --force --timestamp --options runtime \
                  --sign "${APPLE_SIGNING_IDENTITY:-"-"}" \
                  "$binary" || echo "  ⚠️ Failed to sign $(basename "$binary")"
              fi
            done
          fi

          # Also check for binaries in root Resources directory
          for binary_name in yt-dlp ffmpeg; do
            if [ -f "$RESOURCES_PATH/$binary_name" ]; then
              echo "📝 Signing $binary_name..."
              codesign --force --timestamp --options runtime \
                --sign "${APPLE_SIGNING_IDENTITY:-"-"}" \
                "$RESOURCES_PATH/$binary_name" || echo "⚠️ Failed to sign $binary_name"
            fi
          done

          # Sign the entire app bundle with hardened runtime
          echo "📝 Signing app bundle with hardened runtime..."
          codesign --force --deep --timestamp --options runtime \
            --sign "$APPLE_SIGNING_IDENTITY" \
            --entitlements src-tauri/entitlements.plist \
            "$APP_PATH" || {
              echo "❌ Failed to sign app bundle with specific identity"
              echo "⚠️ Using ad-hoc signature as fallback"
              codesign --force --deep --timestamp --options runtime \
                --sign - \
                --entitlements src-tauri/entitlements.plist \
                "$APP_PATH"
            }

          # Verify the signature
          echo "✅ Verifying signature..."
          codesign --verify --deep --strict --verbose=2 "$APP_PATH"
          codesign -dvv "$APP_PATH"
          spctl --assess --type execute --verbose=4 "$APP_PATH" || echo "⚠️ Gatekeeper assessment may fail without notarization"

          # Create a new DMG with the signed app
          echo "📦 Creating DMG with signed app..."
          DMG_DIR="src-tauri/target/universal-apple-darwin/release/bundle/dmg"
          APP_NAME="YT Smart Clip"
          DMG_PATH="$DMG_DIR/${APP_NAME}_signed.dmg"

          # Remove old DMG if exists
          rm -f "$DMG_PATH"

          # Create DMG using hdiutil
          hdiutil create -volname "$APP_NAME" -srcfolder "$APP_PATH" -ov -format UDZO "$DMG_PATH"

          echo "Created signed DMG at: $DMG_PATH"

          # Check if notarization should be performed
          # Set ENABLE_NOTARIZATION=true in repository secrets to enable
          if [ "${{ secrets.ENABLE_NOTARIZATION }}" = "true" ] && [ -n "$APPLE_ID" ] && [ -n "$APPLE_PASSWORD" ] && [ -n "$APPLE_TEAM_ID" ]; then
            echo "📤 Notarization enabled - uploading DMG..."

            # Submit for notarization (async - don't wait)
            SUBMISSION_ID=$(xcrun notarytool submit "$DMG_PATH" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_PASSWORD" \
              --team-id "$APPLE_TEAM_ID" \
              --output-format json | jq -r '.id')

            echo "📋 Notarization submission ID: $SUBMISSION_ID"
            echo "ℹ️  Check status with: xcrun notarytool log $SUBMISSION_ID --apple-id $APPLE_ID --password <app-password> --team-id $APPLE_TEAM_ID"

            # Optional: Wait for notarization (can take 5-30 minutes)
            # Uncomment the following lines to wait for completion:
            # xcrun notarytool wait "$SUBMISSION_ID" \
            #   --apple-id "$APPLE_ID" \
            #   --password "$APPLE_PASSWORD" \
            #   --team-id "$APPLE_TEAM_ID" \
            #   --timeout 30m

            # # Staple the notarization ticket
            # xcrun stapler staple "$DMG_PATH"

            echo "✅ Notarization submitted (async)"
          else
            echo "⚠️ Notarization disabled or credentials not configured"
            echo "   Set ENABLE_NOTARIZATION=true in secrets to enable"
            echo "   DMG is signed but not notarized"
          fi

          echo "✅ Code signing process completed!"

      - name: Upload artifacts (macOS)
        if: matrix.platform == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg
          path: src-tauri/target/universal-apple-darwin/release/bundle/dmg/*_signed.dmg
          if-no-files-found: error

      - name: Upload artifacts (Windows)
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: src-tauri/target/release/bundle/nsis/*-setup.exe
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded artifacts
        run: |
          echo "📦 Downloaded artifacts:"
          ls -R artifacts/

      - name: Prepare release files
        run: |
          mkdir -p release-files

          # Find and copy DMG file
          find artifacts/macos-dmg -name "*.dmg" -exec cp {} release-files/ \;

          # Find and copy EXE installer
          find artifacts/windows-installer -name "*-setup.exe" -exec cp {} release-files/ \;

          echo "📁 Files to be released:"
          ls -lh release-files/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release-files/*
          draft: false
          prerelease: false
          generate_release_notes: true
          name: Release ${{ github.ref_name }}
          body: |
            ## 📥 Download

            ### macOS (Universal Binary - Intel & Apple Silicon)
            Download the `.dmg` file below

            ### Windows (x64)
            Download the `-setup.exe` file below

            ## ✨ What's New

            Check the release notes below for details.

            ---

            🤖 This release was automatically built and signed by GitHub Actions.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
