"use client";

// This function is no longer needed since we use LoadingScreen
// and keep the window visible in tauri.conf.json
export function showWindow() {
  // No-op: window is already visible
  // LoadingScreen component handles the startup experience
}
